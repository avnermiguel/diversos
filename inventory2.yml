plugin: microsoft.ad.ad
server: winserver2025.local.info
user: "{{ lookup('env', 'AD_USER') }}"
password: "{{ lookup('env', 'AD_PASSWORD') }}"
filter: "(objectClass=computer)"
attributes:
  - name
  - sAMAccountName
  - dNSHostName
include_ou_groups: true
