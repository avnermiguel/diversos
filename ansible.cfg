[defaults]
# Remova qualquer linha que aponte para collections_paths locais
# Deixe o AWX gerenciar os caminhos dentro do container

[inventory]
enable_plugins = host_list, script, auto, yaml, ini, toml, microsoft.ad.ad
